# H3 Online Lobby Rating System - Configuration Template
# Copy this file to .env and adjust paths as needed.

# Input Data
# Paths to your local match and player databases (CSV)
MATCHES_CSV=data/matches.csv
PLAYERS_CSV=data/players.csv

# Template grouping definitions (JSON list of template names)
# Defines which templates belong to which group (e.g., JC_Family)
TEMPLATE_GROUPS_JSON=template_groups.json

# Core Settings
RATINGS_SCRIPT=rating_system.py

# Output Structure
# WORKING_DIR is the root for all processed rating data.
# Each group gets its own subdirectory: data/groups/<group_name>/
WORKING_DIR=data/groups

# Filenames within each group directory:
RATINGS_FILENAME=ratings.csv
PRIORS_FILENAME=priors.csv
PARAMS_FILENAME=params.json